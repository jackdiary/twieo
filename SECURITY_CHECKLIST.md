# 배포 전 보안 체크리스트

## ⚠️ 필수 수정 사항

### 1. 환경 변수 (.env)
- [ ] **SECRET_KEY 변경 필요** - 현재 기본값 사용 중
  - **상태**: 프로덕션 서버에 환경 변수로 설정 예정.

- [ ] **DATABASE_URL 보안**
  - **상태**: 프로덕션 서버에 환경 변수로 설정 예정.

- [✅] **WEATHER_API_KEY 보안 (완료)**
  - **조치**: 코드가 환경 변수(`WEATHER_API_KEY`)를 사용하도록 구현된 것을 확인. 키 유출 위험 없음.

### 2. CORS 설정 (main.py)
- [✅] **CORS 제한 완료**
  - **조치**: `twieo.shop` 도메인만 허용하도록 수정 완료.

### 3. API URL 설정 (frontend/config/api.js)
- [✅] **API URL 설정 완료**
  - **조치**: 프로덕션 환경에서 `https://api.twieo.shop`을 바라보도록 수정 완료.

### 4. 데이터베이스 보안
- [ ] **비밀번호 강도**
  - **상태**: 프로덕션 데이터베이스 생성 시 강력한 비밀번호 설정 예정.

### 5. .gitignore 설정
- [✅] **민감 파일 무시 (완료)**
  - **조치**: `.env` 파일이 Git에 포함되지 않도록 설정된 것을 확인하고, 서버 접속 키(`*.pem`)도 무시하도록 규칙을 추가하여 보안 강화.

---

## ✅ 양호한 보안 설정

### 1. 인증 및 권한
- ✅ JWT 토큰 사용
- ✅ 비밀번호 해싱 (bcrypt)
- ✅ 토큰 만료 시간 설정 (30분)
- ✅ 인증 미들웨어 구현

### 2. 입력 검증
- ✅ Pydantic 스키마 검증
- ✅ SQL Injection 방지 (SQLAlchemy ORM 사용)
- ✅ 이메일 형식 검증

### 3. 에러 처리
- ✅ 상세 에러 메시지 숨김
- ✅ HTTP 상태 코드 적절히 사용

---

*이후 내용은 동일하게 유지됩니다.*
