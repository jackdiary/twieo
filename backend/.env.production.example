# 프로덕션 환경 변수 템플릿
# 실제 배포 시 이 파일을 복사하여 .env.production으로 만들고 값을 채우세요

# Database (프로덕션 데이터베이스)
DATABASE_URL=postgresql://username:password@production-host:5432/twieo_db

# JWT Secret (openssl rand -hex 32로 생성)
SECRET_KEY=your-production-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Weather API
WEATHER_API_KEY=your-weather-api-key

# Server
HOST=0.0.0.0
PORT=8000

# Environment
ENVIRONMENT=production

# CORS Allowed Origins (쉼표로 구분)
# 예: ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
